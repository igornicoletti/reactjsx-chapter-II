import styled from 'styled-components';

export const DashboardCtn = styled.div`
  background-color: var(--background);
`;

export const DashboardCtnCtt = styled.div`
  width: 100%;
  margin: 0 auto;;
  max-width: 1120px;
  position: relative;
`;
